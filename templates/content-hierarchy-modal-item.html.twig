<div {{ attributes }}>
  <div class="hierarchy-item-inner">
    {{ fold_button }} {{ item }}
  </div>
  {% if children %}
    <div class="hierarchy-children">{{ children }}</div>
  {% endif %}
</div>
